services:
  backend:
    image: ghcr.io/${IMAGE}:${IMAGE_TAG:-latest}
    container_name: glados-backend
    environment:
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
      - RELEASE_VERSION=${IMAGE_TAG}
      - WEATHER_TOKEN=${WEATHER_TOKEN}
      - ADMIN_IDS=${ADMIN_IDS}
