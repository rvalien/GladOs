services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        RELEASE_VERSION: ${RELEASE_VERSION:-latest}
      tags:
        - backend:${RELEASE_VERSION:-latest}
        - backend:latest
    image: glados-backend:${RELEASE_VERSION:-latest}