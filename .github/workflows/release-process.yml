name: Release Process
on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Номер релиза (25.5, 25.5.1)'
        required: true
      source_branch:
        description: 'Исходная ветка для релиза'
        required: true
        default: 'master'
      target_branch:
        description: 'Целевая ветка для релиза'
        required: true
        default: 'release/prod'

jobs:
  release_process:
    environment: cd-prod
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
      pull-requests: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.inputs.source_branch }}

      - name: Set REPO_NAME in lowercase
        run: |
          echo "REPO_NAME=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Login to GHCR
        run: echo "${{ github.token }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Tag Docker image with version
        run: |
          # Проверяем наличие latest образа
          if ! docker pull ghcr.io/"${{ env.REPO_NAME }}":latest; then
            echo "❌ Latest image not found! Please ensure tests passed on master branch."
            exit 1
          fi

          # Тегируем образ версией релиза
          docker tag ghcr.io/"${{ env.REPO_NAME }}":latest ghcr.io/"${{ env.REPO_NAME }}":v${{ github.event.inputs.version }}
          docker push ghcr.io/"${{ env.REPO_NAME }}":v${{ github.event.inputs.version }}

          echo "✅ Docker image tagged as v${{ github.event.inputs.version }}"

      - name: Create Release Tag
        run: |
          git config user.name "${{ secrets.GIT_USER_NAME }}"
          git config user.email "${{ secrets.GIT_USER_EMAIL }}"
          git tag -a "v${{ github.event.inputs.version }}" -m "Release v${{ github.event.inputs.version }}"
          git push origin "v${{ github.event.inputs.version }}"

      - name: Create Pull Request with version in title
        run: |
          gh pr create \
            --base ${{ github.event.inputs.target_branch }} \
            --head ${{ github.event.inputs.source_branch }} \
            --title "[RELEASE-v${{ github.event.inputs.version }}] Release v${{ github.event.inputs.version }}" \
            --body "Release version **${{ github.event.inputs.version }}**

            **Release Notes:**
            - Version: v${{ github.event.inputs.version }}
            - Source: ${{ github.event.inputs.source_branch }}
            - Target: ${{ github.event.inputs.target_branch }}
            - Docker image: \`ghcr.io/${{ env.REPO_NAME }}:v${{ github.event.inputs.version }}\`

            **Changes:**
            - Docker image tagged as v${{ github.event.inputs.version }}
            - Git tag v${{ github.event.inputs.version }} created

            Пожалуйста:
            1. Проверьте изменения
            2. Утвердите PR
            3. Не унывайте

            **⚠️После слияния, автоматически запустится деплой версии v${{ github.event.inputs.version }} в прод**"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Notify Release Created
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_CHAT_ID }}
          token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          format: markdown
          message: |
            📦 *New Release Prepared*

            🏷️ *Version*: `v${{ github.event.inputs.version }}`
            🌿 *Source*: `${{ github.event.inputs.source_branch }}`
            🎯 *Target*: `${{ github.event.inputs.target_branch }}`
            🐳 *Docker*: `ghcr.io/${{ env.REPO_NAME }}:v${{ github.event.inputs.version }}`

            ⏳ *Waiting for PR approval and merge*

            [Review Pull Request](${{ github.server_url }}/${{ github.repository }}/pulls)
