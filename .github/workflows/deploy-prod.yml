name: Deploy to Production

on:
  push:
    branches:
      - release/prod
  workflow_dispatch:
    inputs:
      version:
        description: 'Версия для деплоя (например: v14.03)'
        required: false
        default: 'latest'

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: cd-prod
    steps:
      - name: Deploy Information
        run: |
          echo "🚀 Начинаем деплой!"
          if [ "${{ github.event.inputs.version }}" != "latest" ] && [ "${{ github.event.inputs.version }}" != "" ]; then
            echo "Версия: v${{ github.event.inputs.version }}"
          else
            echo "Версия: latest (из текущей ветки)"
          fi

      - name: Deploy to Production Server
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.PROD_HOST }}
          username: ${{ secrets.PROD_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            cd ${{ secrets.PROD_APP_PATH }}
            git fetch --all --tags
            
            # Если указана версия и это тег, чекаутим его
            if [ "${{ github.event.inputs.version }}" != "latest" ] && [ "${{ github.event.inputs.version }}" != "" ]; then
              echo "🏷️ Checking out tag: v${{ github.event.inputs.version }}"
              git checkout "v${{ github.event.inputs.version }}"
            else
              # Иначе используем текущую ветку
              CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD)
              echo "📋 Current branch: $CURRENT_BRANCH"
              git pull origin $CURRENT_BRANCH
            fi
            
            echo "🐳 Building Docker images..."
            docker compose build backend
            echo "🔄 Starting services..."
            docker compose up -d
            echo "✅ Deployment completed!"
            docker compose ps

      - name: Notify Success
        if: success()
        run: |
          echo "🎉 Deployment успешно завершен!"
          if [ "${{ github.event.inputs.version }}" != "latest" ] && [ "${{ github.event.inputs.version }}" != "" ]; then
            echo "Версия: v${{ github.event.inputs.version }}"
          else
            echo "Ветка: release/prod"
          fi