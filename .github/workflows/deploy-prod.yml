# deploy-prod.yml
name: Deploy to Production
on:
  push:
    branches:
      - release/prod
  workflow_dispatch:
    inputs:
      version:
        description: 'Версия для деплоя (например: v14.03)'
        required: false
        default: 'latest'

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: cd-prod
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Determine version to deploy
        id: get_version
        run: |
          # Приоритет: inputs -> repository variable -> latest
          if [ "${{ github.event.inputs.version }}" != "latest" ] && [ "${{ github.event.inputs.version }}" != "" ]; then
            VERSION="${{ github.event.inputs.version }}"
            echo "🎯 Используется версия из inputs: $VERSION"
          elif [ "${{ vars.CURRENT_RELEASE_VERSION }}" != "" ]; then
            VERSION="${{ vars.CURRENT_RELEASE_VERSION }}"
            echo "📦 Используется версия из релиза: $VERSION"
          else
            VERSION="latest"
            echo "🔄 Используется latest версия"
          fi
          
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "final_version=$VERSION" >> $GITHUB_ENV
      
      - name: Clear release version (if used)
        if: vars.CURRENT_RELEASE_VERSION != '' && github.event.inputs.version == ''
        run: |
          gh variable delete CURRENT_RELEASE_VERSION || echo "Variable already deleted or doesn't exist"
          echo "🧹 Очищена переменная релиза после использования"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Deploy Information
        run: |
          echo "🚀 Начинаем деплой!"
          echo "Версия: ${{ steps.get_version.outputs.version }}"
          echo "Trigger: ${{ github.event_name }}"
          if [ "${{ github.event_name }}" = "push" ]; then
            echo "Коммит: ${{ github.sha }}"
          fi
      
      - name: Deploy to Production Server
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.PROD_HOST }}
          username: ${{ secrets.PROD_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            cd ${{ secrets.PROD_APP_PATH }}
            git fetch --all --tags
            
            VERSION="${{ steps.get_version.outputs.version }}"
            
            if [ "$VERSION" != "latest" ]; then
              echo "🏷️ Checking out tag: v$VERSION"
              if git checkout "v$VERSION"; then
                echo "✅ Successfully checked out tag v$VERSION"
              else
                echo "❌ Failed to checkout tag v$VERSION, using latest"
                git pull origin release/prod
              fi
            else
              CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD)
              echo "📋 Current branch: $CURRENT_BRANCH"
              git pull origin $CURRENT_BRANCH
            fi
            
            echo "🐳 Building Docker images..."
            docker compose build backend
            echo "🔄 Starting services..."
            docker compose up -d
            echo "✅ Deployment completed!"
            docker compose ps
      
      - name: Notify Success
        if: success()
        run: |
          echo "🎉 Deployment успешно завершен!"
          echo "📌 Версия: ${{ steps.get_version.outputs.version }}"
          echo "🔗 Commit: ${{ github.sha }}"
      
      - name: Show logs
        if: success()
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.PROD_HOST }}
          username: ${{ secrets.PROD_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            docker logs glados-backend-1 --tail 50